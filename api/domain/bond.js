class Bond {

    constructor(precioCompra, precioVenta, ticker, operations) {    
      this.precioCompra = precioCompra
      this.precioVenta = precioVenta
      this.ticker = ticker      
      this.operations = operations;
    }
}  

module.exports = Bond